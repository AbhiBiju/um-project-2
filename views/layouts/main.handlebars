<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Just Tech News</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/tailwind.css">
</head>

<body class="bg-blue">
    <header class="font-semibold text-xl tracking-tight flex-col">
      <nav class="w-full flex justify-between">
        <h1 class="">
          <a href="/" class="text-2xl font-semibold tracking-tight">Book<span class="text-green-500">Me</span></a>
        </h1>
        {{#if loggedIn}}
        <a href="/dashboard">dashboard</a>
        <button id="logout" class="btn-no-style text-2xl font-semibold tracking-tight">Logout</button>
        {{else}}
        <a href="/login" id="loginDisplay" class="text-2xl font-semibold tracking-tight">Login</a>
        {{/if}}
      </nav>
      <ul class="inline-flex justify-between py-2">
        <li class="px-4 text-green-300 hover:text-green-700 transform transition duration-600 hover:scale-125">
          <a href="/">Homepage</a>
        </li>
        <li class="px-4 text-green-300 hover:text-green-700 transform transition duration-600 hover:scale-125">
          <a href="/dashboard">Dashboard</a>
        </li>
        <li class="px-4 text-green-300 hover:text-green-700 transform transition duration-600 hover:scale-125">
          <a href="/club">Book Club</a>
        </li>
        <li class="px-4 text-green-300 hover:text-green-700 transform transition duration-600 hover:scale-125">
          <a href="/map">Near Me</a>
        </li>
        <li class="px-4 text-green-300 hover:text-green-700 transform transition duration-600 hover:scale-125">
          <a href="/profile">Profile</a>
        </li>
      </ul>
    </header>
  <div class="wrapper">
    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>
  
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>